<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.floatingbutton">

    <!-- Разрешение для отображения поверх других приложений -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"  <!-- наша единственная иконка -->
        android:label="@string/app_name"
        android:theme="@style/Theme.FloatingButtonApp">

        <!-- Главная Activity -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Сервис для плавающих кнопок -->
        <service
            android:name=".FloatingButtonService"
            android:exported="false"/>
    </application>
</manifest>
