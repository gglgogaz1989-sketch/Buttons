pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // ⚡ оставляем
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "FloatingButtonApp"
include ':app'
